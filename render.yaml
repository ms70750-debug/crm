services:
  - type: web
    name: bbb-consig-crm-api
    runtime: python
    rootDir: backend
    plan: starter
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: /healthz
    autoDeploy: false
    envVars:
      - key: BBB_AUTH_SECRET
        sync: false
      - key: CORS_ORIGINS
        value: "https://SEU-FRONTEND.vercel.app"
      - key: DATABASE_URL
        value: "sqlite:///./app.db"
      - key: EVOLUTION_API_MODE
        value: "simulation"
